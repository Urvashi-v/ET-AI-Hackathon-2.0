# Runtime dependencies. Pinned to minor versions for reproducible demos.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
psycopg[binary,pool]==3.2.3
neo4j==5.27.0
redis==5.2.1
structlog==24.4.0
orjson==3.10.13
httpx==0.28.1
tenacity==9.0.0
# --- document understanding ---
# pdfplumber (over pdfminer.six) is the parser of record: it yields word-level
# bounding boxes, ruled-table structure, and vector-object counts. pypdf remains
# for the cheap encryption/metadata probe.
pdfplumber==0.11.4
pypdf==5.1.0
python-docx==1.1.2
# --- OCR ---
# pypdfium2 renders PDF pages to raster for the scanned path. BSD/Apache, with
# bundled binaries, so it needs no system libraries (unlike pdf2image+poppler).
# pytesseract drives the tesseract binary installed in the image; it returns
# per-word bounding boxes and per-word confidence, which is what the provenance
# contract requires. Both are inert unless OCR_PROVIDER is set.
pypdfium2==4.30.0
pytesseract==0.3.13
Pillow==10.4.0
# --- embeddings and reranking ---
# fastembed runs real models through ONNX Runtime. Chosen over
# sentence-transformers because it needs no torch: ~90 MB of dependency instead
# of ~2.5 GB, which keeps the image small enough to rebuild repeatedly. The
# models are the real published ones (BAAI/bge-small-en-v1.5 for embeddings,
# Xenova/ms-marco-MiniLM-L-6-v2 cross-encoder for reranking), downloaded once
# and cached; no API key, and no network call after the first run.
fastembed==0.5.1
rapidfuzz==3.11.0
numpy==2.2.1

# OpenCV for P&ID symbol and line detection. The headless build: no GUI bindings,
# no X11 dependency, ~40 MB instead of ~90 MB, and nothing in a server process
# should be opening a window anyway.
#
# Used for classical CV only -- Hough circle and line transforms. Those are real
# detectors with no training data and no learned weights, which is exactly why
# they are the right first step: a deterministic detector whose failures are
# explainable beats a model whose accuracy nobody has measured on this drawing.
opencv-python-headless==4.10.0.84
